// Novartis Oncology — placeholder trials corpora.
// Legacy named exports kept so rag.js imports don't break, but content
// is empty. Hand-curated trial summaries can be added per-product
// later — the RAG path falls back gracefully without them.

export const TRIALS_SOLIRIS = [];           // legacy name — keep stub
export const TRIALS_ULTOMIRIS = [];         // legacy name — keep stub
export const TRIALS_COMPETITORS = [];       // legacy name — keep stub
export const TRIALS_COMPLETED_LANDMARK = []; // legacy name — keep stub

// Forward-looking Novartis exports — populate when needed.
export const TRIALS_KISQALI = [];
export const TRIALS_PLUVICTO = [];
export const TRIALS_SCEMBLIX = [];
